# Minimal runtime deps for running Tensorflow/scripts/generate_tfrecord.py inside .venv
# Tested on Windows + Python 3.11
tensorflow==2.16.1
protobuf<5
pandas
pillow

# Needed to compile TF Models Object Detection API protos (protobuf-4-compatible output).
grpcio-tools==1.56.2
